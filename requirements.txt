# =========================================================
# CORE FRAMEWORK
# =========================================================
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# =========================================================
# HTTP & NETWORK
# =========================================================
requests>=2.31.0
httpx>=0.25.0
aiohttp>=3.8.0
python-multipart>=0.0.6

# =========================================================
# DATA PROCESSING & ML (FULLY INSTALLED)
# =========================================================
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
statsmodels>=0.14.0
scikit-learn>=1.3.0

# =========================================================
# PROPHET (Seasonal Forecasting)
# =========================================================
prophet>=1.1.0
cmdstanpy>=1.0.4
matplotlib>=3.5.0
holidays>=0.25

# =========================================================
# AI & LLM
# =========================================================
groq>=0.4.0
google-genai>=0.2.0              # REPLACED: New Gemini SDK (was google-generativeai)

# =========================================================
# VECTOR EMBEDDINGS & RAG
# =========================================================
sentence-transformers>=2.2.0    # Local embeddings (free, works offline)
pgvector>=0.2.0                 # PostgreSQL vector extension support
psycopg2-binary>=2.9.0          # PostgreSQL adapter

# Optional: Better embeddings (requires API keys)
# openai>=1.0.0                  # OpenAI embeddings (more accurate, costs money)

# =========================================================
# KNOWLEDGE GRAPH (NEW)
# =========================================================
networkx>=3.0.0                 # Graph operations (fallback, no external DB needed)
neo4j>=5.14.0                   # Neo4j driver (optional - for production graph database)

# =========================================================
# UTILITIES
# =========================================================
loguru>=0.7.0
rapidfuzz>=3.0.0
cachetools>=5.0.0
tenacity>=8.0.0
redis>=5.0.0
joblib>=1.3.0
threadpoolctl>=3.2.0

# =========================================================
# VISUALIZATION (Uncommented - for better debugging)
# =========================================================
seaborn>=0.12.0
plotly>=5.0.0                   # UNCOMMENTED - fixes plotly import error

# =========================================================
# DATABASE (PostgreSQL with pgvector for RAG)
# =========================================================
# asyncpg>=0.29.0      # PostgreSQL (async)
# aiomysql>=0.2.0      # MySQL
sqlalchemy>=2.0.0               # ORM
psycopg2-binary>=2.9.0          # PostgreSQL adapter (already above, keeping for clarity)

# =========================================================
# SCHEDULING & TASKS (NEW)
# =========================================================
APScheduler>=3.10.0              # Advanced Python Scheduler for background jobs

# =========================================================
# EXCEL & REPORTING (NEW)
# =========================================================
openpyxl>=3.1.0                  # Excel file support
xlrd>=2.0.0                      # Excel reading (legacy format)
xlsxwriter>=3.1.0                # Excel writing

# =========================================================
# ADDITIONAL UTILITIES (NEW)
# =========================================================
python-dateutil>=2.8.0           # Date parsing utilities
pytz>=2023.3                     # Timezone support
click>=8.1.0                     # CLI utilities
colorama>=0.4.6                  # Terminal colors
tqdm>=4.66.0                     # Progress bars